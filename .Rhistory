setwd("C:/Users/aross4/Desktop/R Folder/obiwanaddone")
install.packages("devtools")
install.packages("roxygen2")
library(devtools)
library(roxygen2)
setwd("C:/Users/aross4/Desktop/R Folder/obiwanaddone")
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) } return(outlier.vec) }
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) } return(outlier.vec) }
for (i in 1:nrow(x))
Alon <- function(N)
{
P <-20
increm <- c(1:(P-1))
sum <- sum(1/increm^N)
part1 <- sum
part2 <- 1/((N-1)*(P^(N-1)))
part3 <- 1/(2*(P^N))
part4 <- N/(24*(P-1)^(N+1))
result <-(part1+part2+part3+part4)
result <- 1/result
return(result)
}
{ outlier.vec[i] <- all(outliers[i,]) } return(outlier.vec) }
debug(for (i in 1:nrow(x)))
debug(for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) } return(outlier.vec) }
testthat(for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) } return(outlier.vec) })
#tukey_multiple <- function(x) {
#  outliers <- array(TRUE,dim=dim(x))
# for (j in 1:ncol(x))
#{
# outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
#  }
# outlier.vec <- vector(length=nrow(x))
testthat(for (i in 1:nrow(x)))
#tukey_multiple <- function(x) {
#  outliers <- array(TRUE,dim=dim(x))
# for (j in 1:ncol(x))
#{
# outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
#  }
# outlier.vec <- vector(length=nrow(x))
testthat(for (i in 1:nrow(x))
#tukey_multiple <- function(x) {
#  outliers <- array(TRUE,dim=dim(x))
# for (j in 1:ncol(x))
#{
# outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
#  }
# outlier.vec <- vector(length=nrow(x))
debug(for (i in 1:nrow(x))
#tukey_multiple <- function(x) {
#  outliers <- array(TRUE,dim=dim(x))
# for (j in 1:ncol(x))
#{
# outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
#  }
# outlier.vec <- vector(length=nrow(x))
debug(for (i in 1:nrow(x)
#tukey_multiple <- function(x) {
#  outliers <- array(TRUE,dim=dim(x))
# for (j in 1:ncol(x))
#{
# outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
#  }
# outlier.vec <- vector(length=nrow(x))
debug (i in 1:nrow(x)
for (i in 1:nrow(x))
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) } return(outlier.vec) }
tukey_multiple <- function(x) {
return(outlier.vec) }}
for (i in 1:nrow(x))
tukey_multiple <- function(x) {
tukey_multiple <- function(x) {
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,])
return(outlier.vec) }
tukey_multiple <- function(x) {
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,])
return(outlier.vec) }
{ outlier.vec[i] <- all(outliers[i,])
{ outlier.vec[i] <- all(outliers[i,])
{ outlier.vec[i] <- all(outliers[i,])
)))
))
)
tukey_multiple <- function(x) {
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,])
return(outlier.vec)
}}
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) } return(outlier.vec) }
tukey_multiple <- function(x) {
return(outlier.vec) }
}
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) }
return(outlier.vec) }
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) }
return(outlier.vec) }
turkey_multiple(x)
turkey_multiple(j)
turkey_multiple(TRUE)
View(tukey_multiple)
View(tukey_multiple)
View(tukey_multiple)
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x)
{ outlier.vec[i] <- all(outliers[i,]) }
return(outlier.vec) })
return(outlier.vec) }
tukey_multiple <- function(x) {
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) }
return(outlier.vec) }
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) }
return(outlier.vec) }
))
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) }
return(outlier.vec) }
tukey_multiple <- function(x) {
outliers <- array(TRUE,dim=dim(x))
for (j in 1:ncol(x))
{
outliers[,j] <- outliers[,j] && tukey.outlier(x[,j])
}
outlier.vec <- vector(length=nrow(x))
for (i in 1:nrow(x))
{ outlier.vec[i] <- all(outliers[i,]) } return(outlier.vec) }
View(tukey_multiple)
Alon <- function(N)
{
P <-20
increm <- c(1:(P-1))
sum <- sum(1/increm^N)
part1 <- sum
part2 <- 1/((N-1)*(P^(N-1)))
part3 <- 1/(2*(P^N))
part4 <- N/(24*(P-1)^(N+1))
result <-(part1+part2+part3+part4)
result <- 1/result
return(result)
}
View(Alon)
install.packages("devtools")
install.packages("devtools")
install.packages("devtools")
install.packages("devtools")
install.packages("devtools")
install.packages("devtools")
install.packages("devtools")
install.packages("devtools")
library(devtools)
library(devtools)
devtools::install_github("alexandriaross/roxygen")
devtools::install_github("alexandriaross/intro_R")
library(roxygen2)
devtools::load_all(".")
library(obiwanaddone)
StarWars
devtools::load_all(".")
load("C:/Users/aross4/Desktop/R Folder/obiwanaddone/data/StarWars.RData")
add_one <- function (Starwars, height) {
taller <- height + 1
return(taller)
}
add_one
taller <-c(StarWars$height + 1)
StarWars$taller <-StarWars[,2]+1
load("C:/Users/aross4/Desktop/R Folder/obiwanaddone/data/StarWars.RData")
add_one <- function (Starwars, height) {
taller <- height + 1
return(taller)
}
taller <-c(StarWars$height + 1)
StarWars$taller <-StarWars[,2]+1
library(obiwanaddone)
StarWars
